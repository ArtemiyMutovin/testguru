<header class="nav">
  <%if user_signed_in? %>
    <h3><%= t('shared.header.welcome', first_name: current_user.first_name, last_name: current_user.last_name ) %></h3>
      <h4><%= link_to t('shared.header.footer'), destroy_user_session_path,
                      data: {"turbo-method": :delete},
                      class: "btn btn-danger ml-3"%></h4>
    <%= link_to t('.badge'), badges_path, class: 'navbar-brand' %>
    <% end %>
</header>
